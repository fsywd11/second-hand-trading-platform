<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.product.mapper.AiChatSessionMapper">

    <resultMap id="SessionMap" type="org.example.ai.POJO.AiChatSession">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="title" property="title" />
        <result column="message_count" property="messageCount" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <insert id="insert" parameterType="org.example.ai.POJO.AiChatSession">
        INSERT INTO ai_chat_session (id, user_id, title, message_count, create_time, update_time)
        VALUES (#{id}, #{userId}, #{title}, #{messageCount}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="org.example.ai.POJO.AiChatSession">
        UPDATE ai_chat_session
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="messageCount != null">message_count = #{messageCount},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_chat_session WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM ai_chat_session WHERE user_id = #{userId}
    </delete>

    <select id="findById" resultMap="SessionMap">
        SELECT * FROM ai_chat_session WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="SessionMap">
        SELECT * FROM ai_chat_session
        WHERE user_id = #{userId}
        ORDER BY update_time DESC
    </select>

</mapper>
