<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.product.mapper.AiChatMessageMapper">

    <resultMap id="MessageMap" type="org.example.ai.POJO.AiChatMessage">
        <id column="id" property="id" />
        <result column="session_id" property="sessionId" />
        <result column="role" property="role" />
        <result column="content" property="content" />
        <result column="sequence" property="sequence" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <insert id="insert" parameterType="org.example.ai.POJO.AiChatMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_message (session_id, role, content, sequence, create_time)
        VALUES (#{sessionId}, #{role}, #{content}, #{sequence}, #{createTime})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO ai_chat_message (session_id, role, content, sequence, create_time)
        VALUES
        <foreach collection="list" item="msg" separator=",">
            (#{msg.sessionId}, #{msg.role}, #{msg.content}, #{msg.sequence}, #{msg.createTime})
        </foreach>
    </insert>

    <delete id="deleteBySessionId">
        DELETE FROM ai_chat_message WHERE session_id = #{sessionId}
    </delete>

    <select id="findBySessionId" resultMap="MessageMap">
        SELECT * FROM ai_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY sequence ASC, id ASC
    </select>

    <select id="findRecentBySessionId" resultMap="MessageMap">
        SELECT * FROM ai_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY sequence DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="countBySessionId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM ai_chat_message WHERE session_id = #{sessionId}
    </select>

</mapper>
