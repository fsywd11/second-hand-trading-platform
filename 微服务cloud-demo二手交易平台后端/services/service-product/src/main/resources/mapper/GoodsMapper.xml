<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.product.mapper.GoodsMapper">

    <sql id="BaseColumnList">
        g.id, g.goods_name AS goodsName, g.goods_pic AS goodsPic, g.category_id AS categoryId,
        g.sell_price AS sellPrice, g.is_new AS isNew, g.goods_status AS goodsStatus, g.stock,
        g.create_time AS createTime, u.nickname AS sellerNickname, c.category_name AS categoryName,
        u.user_pic AS sellerPic, g.seller_id AS sellerId
    </sql>

    <select id="allList" parameterType="org.example.goods.DTO.GoodsQueryDTO" resultType="org.example.goods.VO.GoodsVO">
        SELECT
        <include refid="BaseColumnList"/>
        FROM goods g
        LEFT JOIN user u ON g.seller_id = u.id
        LEFT JOIN shop_category c ON g.category_id = c.id
        <where>
            <if test="goodsName != null and goodsName != ''">
                AND g.goods_name LIKE CONCAT('%', #{goodsName}, '%')
            </if>
            <if test="categoryId != null and categoryId != 0">
                AND g.category_id = #{categoryId}
            </if>
            <if test="goodsStatus != null">
                AND g.goods_status = #{goodsStatus}
            </if>
            <if test="isNew != null">
                AND g.is_new = #{isNew}
            </if>
            <if test="minPrice != null">
                AND g.sell_price <![CDATA[ >= ]]> #{minPrice}
            </if>
            <if test="maxPrice != null">
                AND g.sell_price <![CDATA[ <= ]]> #{maxPrice}
            </if>
            <if test="sellerId != null">
                AND g.seller_id = #{sellerId}
            </if>
        </where>
        ORDER BY CASE WHEN g.goods_status = 1 THEN 0 ELSE 1 END, g.create_time DESC
    </select>

    <select id="list" resultType="org.example.goods.VO.GoodsVO">
        SELECT
        <include refid="BaseColumnList"/>
        FROM goods g
        LEFT JOIN user u ON g.seller_id = u.id
        LEFT JOIN shop_category c ON g.category_id = c.id
        <where>
            <if test="queryDTO.goodsName != null and queryDTO.goodsName != ''">
                AND g.goods_name LIKE CONCAT('%', #{queryDTO.goodsName}, '%')
            </if>
            <if test="categoryIds != null and categoryIds.size() > 0">
                AND g.category_id IN
                <foreach collection="categoryIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="queryDTO.goodsStatus != null">
                AND g.goods_status = #{queryDTO.goodsStatus}
            </if>
            <if test="queryDTO.sellerId != null">
                AND g.seller_id = #{queryDTO.sellerId}
            </if>
        </where>
        ORDER BY g.create_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <update id="update" parameterType="org.example.goods.POJO.Goods">
        UPDATE goods
        <set>
            <if test="goodsName != null and goodsName != ''">goods_name = #{goodsName},</if>
            <if test="goodsDesc != null">goods_desc = #{goodsDesc},</if>
            <if test="goodsPic != null">goods_pic = #{goodsPic},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="originalPrice != null">original_price = #{originalPrice},</if>
            <if test="sellPrice != null">sell_price = #{sellPrice},</if>
            <if test="goodsStatus != null">goods_status = #{goodsStatus},</if>
            <if test="isNew != null">is_new = #{isNew},</if>
            <if test="stock != null">stock = #{stock},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE goods
        SET goods_status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <insert id="insertGoodsImages">
        INSERT INTO goods_image (goods_id, image_url, ext_info, create_time)
        VALUES
        <foreach collection="imageList" item="image" separator=",">
            (#{goodsId}, #{image.imageUrl}, #{image.extInfo}, NOW())
        </foreach>
    </insert>

    <select id="findSellerByUserId" parameterType="java.lang.Integer" resultType="org.example.user.VO.BuyerViewSellerVO">
        SELECT
        u.id AS sellerId,
        u.nickname AS sellerNickname,
        u.user_pic AS sellerAvatar,
        IFNULL(COUNT(g.id), 0) AS publishGoodsCount
        FROM user u
        LEFT JOIN goods g ON g.seller_id = u.id
        WHERE u.id = #{id}
        GROUP BY u.id, u.nickname, u.user_pic
    </select>

    <select id="listByIds" resultType="org.example.goods.VO.GoodsVO">
        SELECT
        <include refid="BaseColumnList"/>
        FROM goods g
        LEFT JOIN user u ON g.seller_id = u.id
        LEFT JOIN shop_category c ON g.category_id = c.id
        WHERE g.id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY g.create_time DESC
    </select>
</mapper>
