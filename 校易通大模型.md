# 校易通大模型

## 一.后端启动

### 1.后端代码容器更新重新打包镜像

#### 步骤 1：停止后端容器（可选，构建时会自动覆盖，加了更稳妥）

```
docker-compose stop jeecg-boot-system
```

#### 步骤 2：重新构建后端镜像

```
mvn clean install -Pdocker -DskipTests
```

#### 步骤 3：重新创建并启动后端容器（比 restart 更彻底，加载新镜像）

```
# up -d 会检测镜像是否更新，自动用新镜像重建容器并后台运行
docker-compose up -d jeecg-boot-system
```

✅ 优势：会重新创建容器，彻底加载新镜像的所有内容（包括依赖、配置、代码），无缓存问题。

## 2.github推送

~~~git
# 1. 暂存所有本地修改（若有自己改的代码，必执行）
git add .
# 2. 提交暂存的修改（备注可自定义，比如「初始化项目，基于JeecgBoot开发二手交易平台」）
git commit -m "初始化项目，迁移到自己的仓库"
# 3. 关联本地主分支和远程主分支（JeecgBoot默认主分支是main/master，按需选，执行一次即可）
git branch --set-upstream-to=origin/main main
# 若原项目分支是master，替换为：git branch --set-upstream-to=origin/master master
# 4. 推送到自己的GitHub仓库（-u 表示绑定分支，后续推代码直接git push即可）
git push -u origin main
# 若分支是master，替换为：git push -u origin master
~~~

